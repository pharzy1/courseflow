name: Catalog and enrollment refresh
on:
  workflow_dispatch:
  schedule:
    - cron: "7,22,37,52 * * * *"
concurrency:
  group: courseflow-data-refresh
  cancel-in-progress: false
permissions:
  contents: read
jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Invoke production ingestion worker
        env:
          COURSEFLOW_REFRESH_SECRET: ${{ secrets.COURSEFLOW_REFRESH_SECRET }}
        run: |
          response=$(curl --fail-with-body --retry 3 --retry-all-errors --retry-delay 5 --max-time 300 -X POST \
            -H "Authorization: Bearer $COURSEFLOW_REFRESH_SECRET" \
            https://courseflowplanner.com/api/ops/catalog-refresh)
          echo "$response" | jq -e '.source | contains("official catalog metadata")'
          echo "$response" | jq -e '.quality.sampleSize >= 250 and .quality.coverage >= 0.99'
      - name: Confirm refresh mode
        run: echo "Production worker completed full catalog pagination, official metadata quality gate, and Neon enrollment observation."
      - name: Surface terminal refresh failure
        if: failure()
        run: echo "::error::CourseFlow production refresh failed after all retry attempts. Inspect the worker response and refresh_runs evidence."
