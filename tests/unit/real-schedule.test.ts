import assert from "node:assert/strict";import test from "node:test";
import { generateConflictFreeSchedules,meetingsConflict } from "../../lib/real-schedule";
import type { CatalogRecord } from "../../lib/data/types";
const meeting=(day:string,start:string,end:string)=>({days:[day],startTime:start,endTime:end,location:null,instructors:[]});
const record=(id:string,code:string,sectionNumber:string,start:string,end:string):CatalogRecord=>({id,courseId:code,code,subject:code.split(" ")[0],number:code.split(" ")[1],title:code,description:"",department:"TEST",unitsMin:4,unitsMax:4,level:null,requirements:[],prerequisites:[],crossListings:[],term:"Fall 2026",sectionNumber,component:"LEC",meetings:[meeting("Monday",start,end)],enrolled:10,capacity:20,waitlisted:0,waitlistCapacity:0,averageGrade:null,medianGrade:null,gradeSampleSize:0,provenance:{sourceId:"test",sourceName:"Test",sourceUrl:"https://example.com",official:false,retrievedAt:"2026-09-02T00:00:00Z",license:null}});
test("meeting conflicts require the same day and overlapping intervals",()=>{assert.equal(meetingsConflict(meeting("Monday","10:00:00","11:00:00"),meeting("Monday","10:30:00","11:30:00")),true);assert.equal(meetingsConflict(meeting("Monday","10:00:00","11:00:00"),meeting("Tuesday","10:30:00","11:30:00")),false);});
test("generator chooses a conflict-free section alternative for every course",()=>{const schedules=generateConflictFreeSchedules([record("a1","DATA C100","001","10:00:00","11:00:00"),record("a2","DATA C100","002","12:00:00","13:00:00"),record("b1","COMPSCI 61B","001","10:30:00","11:30:00")]);assert.equal(schedules.length,1);assert.deepEqual(schedules[0].sections.map(item=>item.id),["a2","b1"]);});
