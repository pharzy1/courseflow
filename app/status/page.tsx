"use client";
import Link from "next/link";import { useEffect,useState } from "react";
type Health={status:string;checkedAt:string;mode:string;responseMs?:number;database:{status:string;latencyMs:number|null};catalog:{sections:number;courses:number;snapshots:number;lastRefresh:string|null;freshness:string;ageMinutes?:number|null};alerts:{watches:number;generated:number;emailConfigured:boolean};checks:Array<{name:string;status:string;detail:string}>};
export default function StatusPage(){const [health,setHealth]=useState<Health|null>(null),[error,setError]=useState("");useEffect(()=>{fetch("/api/health",{cache:"no-store"}).then(async response=>{if(!response.ok)throw new Error("Operational telemetry unavailable");return response.json() as Promise<Health>;}).then(setHealth).catch(cause=>setError(cause.message));},[]);return <main className="status-page"><header className="catalog-top"><Link href="/" className="brand"><span className="brand-mark">CF</span><span>Course<span>Flow</span></span></Link><div><span className="provenance-pill">ENGINEERING EVIDENCE</span><Link href="/catalog">Catalog</Link><Link href="/roadmap">Pathfinder</Link></div></header><section className="status-hero"><div><span className="kicker">LIVE SYSTEM TELEMETRY</span><h1>Trust,<br/><em>made observable.</em></h1><p>Public evidence for data freshness, infrastructure health, and alert readiness.</p></div>{health?<div className={`system-state ${health.status}`}><i/><span>System status</span><strong>{health.status}</strong></div>:null}</section>{error?<section className="status-error" role="alert">{error}</section>:!health?<section className="history-state" aria-live="polite">Loading production telemetry…</section>:<section className="status-shell"><div className="status-metrics"><article><span>Neon latency</span><strong>{health.database.latencyMs}ms</strong><small>{health.database.status}</small></article><article><span>Catalog sections</span><strong>{health.catalog.sections.toLocaleString()}</strong><small>{health.mode} adapter</small></article><article><span>Enrollment observations</span><strong>{health.catalog.snapshots.toLocaleString()}</strong><small>time-series records</small></article><article><span>Data freshness</span><strong>{health.catalog.freshness}</strong><small>{health.catalog.ageMinutes??"—"} minutes old</small></article></div><section className="status-checks"><div><span className="kicker">AUTOMATED CHECKS</span><h2>Production readiness</h2><p>These checks are derived from live infrastructure—not static marketing claims.</p></div><div>{health.checks.map(check=><article key={check.name}><i className={check.status}/><div><b>{check.name}</b><span>{check.detail}</span></div><strong>{check.status}</strong></article>)}</div></section><section className="status-proof"><article><span>Watched sections</span><b>{health.alerts.watches}</b></article><article><span>Alerts generated</span><b>{health.alerts.generated}</b></article><article><span>Email provider</span><b>{health.alerts.emailConfigured?"Configured":"Pending"}</b></article><article><span>Health response</span><b>{health.responseMs??"—"}ms</b></article></section><p className="status-timestamp">Checked {new Date(health.checkedAt).toLocaleString()} · refresh this page for a new measurement.</p></section>}</main>}
